<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.brandon.dating_app">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/data_rater"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/DataRater">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login"
            android:theme="@style/DataRater" />
        <activity
            android:name=".FiltersActivity"
            android:label="@string/title_activity_filters"
            android:theme="@style/DataRater" />
        <activity
            android:name=".SwipingActivity"
            android:theme="@style/DataRater" />
        <activity
            android:name=".RaterActivity"
            android:label="@string/title_activity_rater"
            android:theme="@style/DataRater" />
        <activity android:name=".RegisterActivity" />
        <activity
            android:name=".Register2Activity"
            android:label="@string/title_activity_register2"
            android:theme="@style/DataRater" />
        <activity
            android:name=".AboutMe"
            android:theme="@style/DataRater" />
        <activity
            android:name=".AboutThem"
            android:theme="@style/DataRater" />
        <activity
            android:name=".ChatActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MatchesActivity"
            android:theme="@style/DataRater" />

    </application>

</manifest>